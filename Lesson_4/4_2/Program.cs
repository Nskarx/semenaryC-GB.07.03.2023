// Задача 2: Напишите программу, которая
// принимает на вход число N и выдаёт
// произведение чисел от 1 до N.
// 4 -> 24
// 5 -> 120
 Console.Write("Введите число N: ");
        int n = int.Parse(Console.ReadLine()!);

        int product = 1;

        for (int i = 1; i <= n; i++)
        {
            product *= i;
        }

        Console.WriteLine($"Произведение чисел от 1 до {n}: {product}");
        
// 1.Сначала мы запрашиваем у пользователя число N с помощью Console.ReadLine().
// 2.Затем мы инициализируем переменную product в единицу, так как умножение на 1 не изменяет значение числа.
// 3.Далее мы создаем цикл for, который проходит от 1 до N. На каждом шаге цикла мы умножаем product на текущее значение i.
// 4.После того, как цикл закончился, мы выводим результат с помощью Console.WriteLine().

// В контексте данного кода, product является переменной типа int, которая используется для хранения текущего значения произведения чисел от 1 до N.
//  В начале программы ей присваивается значение 1, так как умножение на 1 не меняет значения. Затем, в цикле от 1 до N, 
//  на каждой итерации значение i умножается на текущее значение product и результат присваивается переменной product.
//   В конце цикла product содержит произведение чисел от 1 до N.